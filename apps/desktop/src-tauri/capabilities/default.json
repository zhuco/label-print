{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the desktop application.",
  "windows": ["main"],
  "permissions": ["core:default", "updater:default"]
}
